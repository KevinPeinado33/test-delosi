import styles from '../page.module.scss';

const Auth = () => {
    return (
        <main className={styles.main}>
            <h1>hola esto es una pruebita</h1>
        </main>
    );
}

export default Auth;
